import 'package:flutter/material.dart';

class TodoTasks extends StatelessWidget {
  const TodoTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Todo TAsks")));
  }
}
