import 'package:flutter/material.dart';

class StudentPrizes extends StatelessWidget {
  const StudentPrizes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Student Prizes")));
  }
}
